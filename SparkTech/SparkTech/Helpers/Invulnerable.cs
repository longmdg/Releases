/*
namespace SparkTech.Helpers
{
    using System.Linq;

    using LeagueSharp;

    public enum AttackType
    {
        
    }

    public static class Invulnerable
    {
        public static bool IsInvulnerable(this Obj_AI_Hero hero)
        {
            if (hero.IsInvulnerable || !hero.IsValid || hero.IsDead || !hero.IsTargetable)
            {
                return true;
            }

            if (hero.Buffs.Any(buff => buff.Type == BuffType.Invulnerability || buff.Type == ))
            {
                return true;
            }
        }
    }
}
*/